# Allow opening any 'source'd file, even if not specified as input
external-sources=true

# Specify the shell to use
shell=bash

# Rules
disable=SC2236 # Allow [ ! -z foo ] instead of suggesting -n
disable=SC2001 # Allow string="string" ; echo "$string" | sed -e "s/ir/ri/"
disable=SC2317 # Allow Command appears to be unreachable
disable=SC2034 # Allow unused variables
disable=SC1090 # ShellCheck can't follow non-constant source. Use a directive to specify location.
disable=SC2155 # Declare and assign separately to avoid masking return values.
disable=SC2164 # Use 'pushd ... || exit' or 'pushd ... || return' in case pushd fails.
